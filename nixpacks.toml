[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --prefix server --legacy-peer-deps"]

[phases.build]
cmds = ["cd server && npx prisma generate"]

[start]
cmd = "node server/dist/index.js"
